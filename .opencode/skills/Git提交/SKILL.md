---
name: "Git提交"
description: "为项目创建详细、结构化的git提交信息。自动分析当前分支从新建点开始的所有提交，生成变更影响汇总表格，检查问题延续性。当用户说'提交''commit''git commit''提交代码'时触发。**禁止在master/test/dev分支使用。**"
tags: ["git", "commit", "conventional-commits", "branch-analysis", "impact-table"]
context: fork
---

# Git提交

> 全局参考：全局 `git-commit` 技能（Conventional Commits 格式规范）
> 项目规范：`.opencode/rules/Git工作流.md`
> 接口文档：`docs/migrated-java-apis/`
> 业务流程文档：`docs/业务流程说明文档/`
> 数据库变更：`sql/init-auth-run-schema.sql` + `sql/数据库表结构更新历史/`
>
> **核心增强**（相比全局 git-commit）：
> 1. 分支保护：禁止在 master / test / dev 分支提交
> 2. 全分支提交分析：从**当前分支的新建点**开始分析所有提交
> 3. 变更影响汇总表格：每个提交标注问题、接口、业务流程、前端影响、表结构影响
> 4. 问题延续性检查：识别后续提交是否是之前提交的延续修复
> 5. 编译与测试门禁：提交前强制编译检查 + 测试检查 + 第三方 API 调用屏蔽检查

---

## 预注入上下文

以下数据由 opencode 在加载技能时自动执行命令并注入，无需手动运行：

- 当前分支: !`git branch --show-current`
- 暂存文件列表: !`git diff --cached --name-only 2>/dev/null || echo "(无暂存文件)"`
- 暂存变更统计: !`git diff --cached --stat 2>/dev/null || echo "(无暂存变更)"`
- 未暂存文件: !`git diff --name-only 2>/dev/null || echo "(无)"`
- 上次提交完整信息: !`git log -1 --format=%B 2>/dev/null || echo "(仓库无提交记录)"`
- 远程基准分支: !`(git branch -r 2>/dev/null | grep -q "origin/master" && echo "origin/master") || (git branch -r 2>/dev/null | grep -q "origin/main" && echo "origin/main") || echo "NEED_FETCH"`
- 最近提交: !`git log --oneline -5 2>/dev/null || echo "(无)"`

---

## 快速检查清单（执行前过一遍，防止漏步骤）

| 序号 | 步骤 | 最易遗漏 |
|------|------|---------|
| 1 | 从预注入上下文读取当前分支，确认非 master/test/dev | |
| 2 | 从预注入上下文读取暂存/未暂存状态，确认有变更可提交 | |
| 3 | 并行派发 3 个 Explore 子代理（diff分析 / 分支历史 / 上次表格） | |
| 4 | 等待子代理返回，检查延续性，合成表格 | |
| 5 | 生成分支变更影响汇总表格（继承上次 + 追加/更新本次） | |
| 6.1 | `mvn test-compile` 编译通过 | |
| 6.2 | `mvn test` 测试通过 | |
| 6.3 | 第三方调用屏蔽检查（@Disabled / Mock / exclude） | |
| 7 | 按模板生成完整提交信息（含继承后的表格） | |
| 8 | 预览确认 → `git add` + `git commit` | |

---

## 执行流程

按以下八个步骤逐项执行，缺失任一项视为未完成。

---

### 第一步：分支保护检查

直接从预注入上下文中读取当前分支名。

- [ ] 如果当前分支是 `master`、`test` 或 `dev`（不区分大小写），**直接终止并提示**：
  > 当前分支 `<branch-name>` 是集成分支（integration branch），不是功能分支。集成分支上的提交总结没有意义——这些分支的提交来源混杂，无法提炼出统一的变更主题。请切换到功能分支（feature branch）后再使用本技能。
- [ ] 通过后记录当前分支名，后续步骤会用到。

---

### 第二步：检查工作区状态

直接从预注入上下文中读取暂存/未暂存文件列表。

- [ ] 如果没有任何变更（无 staged 也无 unstaged），提示用户当前工作区干净，无需提交，流程终止。
- [ ] 如果只有 unstaged 变更，无 staged 变更，提示用户是否需要 `git add` 暂存文件。
- [ ] 列出即将提交的文件清单（来自预注入的暂存文件列表）。
- [ ] 用户确认文件清单无误后，继续下一步。

---

### 第三步：并行派发 3 个 Explore 子代理分析

> **关键**：3 个子代理在同一条消息中并行派发，不可串行执行。

#### Agent A (Explore)：分析暂存变更详情

**任务**：分析 `git diff --cached` 的完整 diff 内容。

- [ ] 逐文件分析变更：
  - **文件路径** → 判断所属模块
  - **变更类型** → 新增 / 修改 / 删除
  - **变更意图** → 新功能 / Bug修复 / 重构 / 文档 / 配置 / 测试 / 数据库变更
- [ ] 识别关键变更：Controller / DTO / Entity / SQL / SSE / 网关配置
- [ ] 返回结构化输出：模块归属、变更文件清单、接口路径列表、DTO/Entity 变更摘要、变更意图判断。

#### Agent B (Explore)：分析分支历史与延续性

**任务**：基于预注入上下文的远程基准分支，分析当前分支从分叉点以来的所有提交，检查问题延续性。

- [ ] 用 merge-base 找真正的分叉点
- [ ] 遍历分叉后的提交列表
- [ ] 将本次 staged 变更的文件列表与历史提交的文件集合做交集对比
- [ ] 返回：分叉点 hash、提交列表、延续性判定结果、提交总数。

#### Agent C (Explore)：提取上次提交的表格行

**任务**：从预注入上下文的上次提交完整信息中，提取"## 分支变更汇总表格"段落的表格行。

- [ ] 提取表格行，逐行原样返回
- [ ] 如果上次提交中没有该段落，返回空并注明"分支首个提交，无历史表格"

---

### 第四步：等待子代理返回，合成结果

- [ ] 汇总 Agent A 输出，确定本次变更的核心主题和影响范围。
- [ ] 汇总 Agent B 输出，确认延续性关系。
- [ ] 汇总 Agent C 输出，作为表格继承的基础。
- [ ] 统计分支提交数量：如果超过 20 个，提示用户分支可能过大。

---

### 第五步：生成分支变更影响汇总表格

> **核心原则**：表格从**当前分支新建点**开始累计。每次提交时：
> - **新问题** → 追加新行
> - **延续修复** → **不追加新行，在原行"问题"列用 `→` 追加修复说明**

- [ ] **5.1 继承上次表格**：将 Agent C 返回的所有表格行原样保留
- [ ] **5.2 分析本次变更**：判断本次变更是新增问题还是延续修复

**表格格式**：

```
| 问题 | 涉及接口 | 涉及业务流程 | 是否涉及前端 | 是否涉及表结构/数据调整 |
|------|---------|-------------|-------------|---------------------|
```

**各列判断依据**：

1. **问题** — 该提交解决了什么问题。聚焦"为什么"而非"做了什么"
2. **涉及接口** — HTTP方法 + 路径，不涉及写 `—`
3. **涉及业务流程** — 从模块名 + 变更路径 + 文档推断
4. **是否涉及前端** — 是/否。DTO 结构变更 / SSE 事件变更 / 路径变更 / 新错误码
5. **是否涉及表结构/数据调整** — 是/否。SQL 文件变更 / JPA Entity 字段变更 / 数据迁移

**排除类型**：`docs:`/`chore:`/`ci:`/`.opencode/` 目录变更、merge commits 不纳入表格。

---

### 第六步：编译与测试门禁

> **强制门禁**：以下三项全部通过后才允许进入第七步。

#### 6.1 编译检查

- [ ] 执行 `mvn -pl <module1>,<module2> -am test-compile`
- [ ] 编译失败 → **终止流程**

#### 6.2 测试检查

- [ ] 执行 `mvn -pl <module1>,<module2> -am test`
- [ ] 测试失败 → **终止流程**

#### 6.3 第三方调用屏蔽检查

- [ ] 扫描测试文件中的外部依赖风险模式
- [ ] 发现未屏蔽的第三方调用 → 警告用户

---

### 第七步：生成完整提交信息

基于以上分析，生成完整的提交信息。格式如下：

```
<type>(<scope>): <简短描述>

## 变更概述
- 用 3-5 个要点概括本次变更的核心内容

## 详细说明
- 具体的代码变更和实现细节

## 影响范围
- 受影响的模块或功能

## 分支变更汇总表格（`<当前分支名>` 累计，起点 `<基准分支>` @ `<merge-base-short-hash>`）

| 问题 | 涉及接口 | 涉及业务流程 | 是否涉及前端 | 是否涉及表结构/数据调整 |
|------|---------|-------------|-------------|---------------------|

> 分支提交总数：N 个
> 其中数据库变更提交：M 个 | 涉及前端变更提交：K 个
> 延续修复：`abc1234` 是 `def5678` 的后续修复 — 修复了xxx问题（如无则省略此行）

## 测试说明
- 已执行的测试 / 验证步骤

## 相关信息
- 关联 issue/PR / 协作者 / 参考文档
```

**提交类型（type）**：

| Type | 用途 | Type | 用途 |
|------|------|------|------|
| `feat` | 新功能 | `fix` | Bug 修复 |
| `refactor` | 重构 | `docs` | 文档 |
| `test` | 测试 | `chore` | 构建/工具 |
| `ci` | CI/CD | `perf` | 性能优化 |
| `revert` | 回滚 | | |

**范围（scope）**：优先用模块名，跨模块用 `cross-module` 或省略。

---

### 第八步：展示预览并确认，执行提交

- [ ] 展示完整的提交信息预览
- [ ] 列出即将提交的文件清单
- [ ] 重点突出分支变更汇总表格
- [ ] 用户确认后执行 `git add` + `git commit`

---

## 常见遗漏点

1. **在集成分支上使用** → 在 master/test/dev 执行提交
2. **子代理串行执行** → 3 个 Explore 子代理应在同一条消息中并行派发
3. **分支起点判断错误** → 未用 `git merge-base origin/master HEAD`
4. **提交前未编译/未跑测试** → 编译和测试失败不得提交
5. **第三方调用未屏蔽** → 真实 HTTP/API Key 会导致 CI 失败或产生费用
6. **汇总表格混入无业务影响的变更** → `docs:`/`chore:`/`ci:`/`.opencode/` 目录变更不纳入
7. **"是否涉及前端"误判** → 以接口契约是否变更为准
8. **"是否涉及表结构"遗漏** → Entity 字段变更也应标记为"是"
9. **延续性关系遗漏** → 延续修复不追加新行，在原行"问题"列用 `→` 链式追加
10. **"问题"列写代码操作** → 应写业务问题
11. **汇总表格未继承上次提交的行** → 通过 Agent C 读取上次提交信息中的表格行
12. **DDL 依赖顺序未标注** → 涉及表结构变更时补充说明
13. **分支提交数过多未提示** → 超过 20 个时提示考虑拆分 PR

---

## 分支保护说明

本技能禁止在以下分支使用：

| 分支名 | 原因 |
|--------|------|
| `master` | 主分支，提交来源混杂，无法提炼统一主题 |
| `test` | 测试集成分支，包含多个功能的合并提交 |
| `dev` | 开发集成分支，提交来自不同开发者的不同功能 |

如需在这些分支提交：
- **紧急热修复**：从 master 创建 hotfix 分支
- **集成分支合并**：使用 PR merge / `git merge`

---

**任务**：分析当前分支的所有变更（含全分支提交历史），生成详细的提交信息并执行提交。
